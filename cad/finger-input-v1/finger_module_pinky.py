"""Pinky preset entry point with its independent lever ratio and geometry."""

from finger_module_common import PRESETS, generate_preset


if __name__ == "__main__":
    generate_preset(PRESETS["pinky"])
