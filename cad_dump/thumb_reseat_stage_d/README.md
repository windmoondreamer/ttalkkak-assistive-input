# Do not use `Joystick_1_stage_d.stl`

The STL in this folder was copied from an older browser download while testing
the Onshape export UI.  Its timestamp predates the THUMB reseat write and it is
**not** a Stage-D export.  It is excluded from every audit and render.

The authoritative result is the live Main workspace plus
`cad_dump/thumb_reseat_final_audit.json`.
