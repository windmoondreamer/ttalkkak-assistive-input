# OneGrip Play 검지·엄지 구조 및 출력성 검증

검증일: 2026-08-21  
대상: `ITS1105_RIGHT_HAND_8BTN_FINAL` 및 오른손 핸들 30-part STL 출력 세트

## 1. 결론

| 항목 | 판정 |
|---|---|
| STL 파일 무결성 | PASS — 30개 모두 닫힌 manifold solid, 퇴화 삼각형 0 |
| 검지부 CAD 간섭 | 조건부 PASS — 실측한 ITS-1105 한 샘플 기준 |
| 검지부 일반 FDM 기능 출력 | HOLD |
| 엄지 버튼 캡 단품 출력 | PASS에 가까움 |
| 엄지 조이스틱 장착부 출력 | SUPPORT/REDESIGN 필요 |
| 현재 한 판 STL의 무서포트 출력 | FAIL |
| 인체공학 검증 | 미완료 — hand phantom 및 사용자 파지 시험 없음 |

현재 STL은 **슬라이서에서 열리는 유효한 형상**이지만, `READY` 표기는 베드 패킹과 STL 무결성에만 해당한다.
현 상태 그대로 한 판에 올려 기능 부품까지 안정적으로 FDM 출력된다는 뜻은 아니다.

## 2. 검지 4버튼 구조

### 현재 구조

- 스위치: ITS-1105-6mm 4개
- 실측 스위치 본체: 약 `6.12 × 6.05 × 3.56mm`
- 포켓: `6.40 × 6.40mm`
- 측면 여유: X 약 `0.11mm/side`, Y 약 `0.14mm/side`
- 캡: I1~I4 독립 부품
- 뒤쪽 고정: I1~I3 공용 retainer + I4 별도 retainer
- 스페이서: `3.6 × 3.6 × 2.44mm` 4개
- 최소 divider: `0.807mm`
- 최소 structural terminal web: `1.553mm`
- 캡 usable travel: 약 `0.384~0.400mm`
- 캡과 스위치 축 편차: I1~I3 약 `17.2°`

### 장점

- 곡면 쉘을 따라 버튼이 배치되어 외형이 컴팩트하다.
- CAD상 스위치 본체, 나사, retainer 간 충돌은 해소돼 있다.
- ITS-1105 한 SKU로 검지·중지 구조를 통일할 수 있다.
- 단품 STL은 모두 manifold이고 캡·스페이서는 평평하게 출력 가능하다.

### 위험

1. `0.807mm` divider는 0.4mm 노즐에서 사실상 약 2개 압출선뿐이다. 반복 클릭과 조립 하중을 받는 구조로는 여유가 작다.
2. 포켓 여유 `0.11/0.14mm per side`는 FDM 수축·과압출·곡면 방향 오차에 민감하다. CAD상 들어가더라도 실제 출력에서는 끼이거나 헐거울 수 있다.
3. 캡 이동량이 0.4mm 미만이라 서포트 자국, 코끼리발, Z seam, 후가공 오차가 작동 불량으로 바로 이어질 수 있다.
4. I1~I3 캡 표면과 스위치 축이 약 17.2° 어긋나 lateral scrub가 생길 가능성이 있다.
5. 작은 스페이서 4개와 복잡한 retainer는 조립 순서와 부품 분실 위험을 늘린다.
6. 검증에 사용된 ITS-1105 치수는 단일 실물 샘플이다. lot 편차와 단자 뿌리 형상은 아직 확정되지 않았다.

### 검지부 판정

- Tough resin/SLA: **조건부 제작 가능**. 다만 얇은 divider와 screw ear의 취성 확인 필요.
- 0.4mm FDM: **현 상태 비권장**.
- 0.2mm 노즐 FDM: coupon 통과 시 가능하지만 조립성과 내구성은 별도 검증 필요.

## 3. 엄지 구조

### 현재 구조

- HW504_B 계열 2축 조이스틱 1개
- 출력 부품 `Small_joystick_attachment`: 약 `14 × 14 × 11mm`
- 조이스틱 구멍: Ø15mm
- 조이스틱 캡 상단 지름: 14mm
- 조이스틱 핀 기준: `3 × 4 × 8mm`
- 설계 tolerance: `0.075mm`
- 원본 엄지 버튼: 8개, 3개 행
- 버튼 클러스터 폭: 약 30mm
- 조이스틱 중심에서 버튼 행까지 약 `24.1 / 30.1 / 41.1mm`

### 장점

- 엄지 조이스틱과 버튼을 동일 전면 패널에 모아 손가락 이동을 줄이려는 구조다.
- 기존 버튼 캡 8개는 각각 manifold이고, 현재 방향에서 대부분 무서포트 출력 가능하다.
- 엄지 조이스틱 장착부가 별도 부품이라 교체 가능하다.

### 위험

1. 조이스틱에서 가장 먼 버튼 행이 41.1mm 떨어져 있다. 손을 고정한 채 모든 버튼을 누를 수 있는지는 아직 검증되지 않았다.
2. 8버튼이 30mm 폭에 밀집해 시각 확인 없이 버튼을 구분하기 어렵고 오입력 가능성이 있다.
3. `0.075mm` 조이스틱 결합 tolerance는 일반 FDM에 지나치게 타이트하다.
4. HW-504 호환품은 제조사별 기판·스틱 높이 편차가 있을 수 있으므로 CAD 모델만으로 fit을 확정할 수 없다.
5. `Small_joystick_attachment`는 현재 방향뿐 아니라 조사한 안정 방향들에서도 약 `120mm²`의 심한 하향면이 남아 서포트 또는 형상 변경이 필요하다.
6. 현재 자료에는 실제 thumb reach envelope, 엄지 관절각, 조이스틱 조작 중 버튼 오입력 시험이 없다.

## 4. STL 기하·출력 방향 감사

현재 패킹 방향의 45° 초과 하향면을 기하적으로 계산했다. 이 값은 실제 slicer의 서포트 부피가 아니라
현재 방향이 얼마나 불리한지 비교하는 지표다.

| 부품 | 현재 크기 mm | 현재 심한 하향면 | 더 나은 방향의 심한 하향면 | 판정 |
|---|---:|---:|---:|---|
| Joystick_1 shell | `180.2×67.3×38.8` | `4597mm²` | `1867mm²` | 현재 방향 support 다량 필요 |
| Joystick_2 shell | `180.0×66.9×38.8` | `4852mm²` | `1841mm²` | 현재 방향 support 다량 필요 |
| Backplate | `40.7×65.9×13.0` | `1596mm²` | `377mm²` | 세워서 출력하는 편이 유리 |
| Shared retainer | `30.1×25.2×22.0` | `286mm²` | `142mm²` | support 또는 분할 필요 |
| I4 retainer | `12.4×10.6×5.1` | `53mm²` | `30mm²` | 작은 support 필요 |
| Small joystick attachment | `14×14×11` | `120mm²` | `120mm²` | 형상 자체가 support 요구 |
| M4 cap | `8.0×8.0×3.6` | `55.7mm²` | `5.0mm²` | 현재 패킹 방향 오류에 가까움 |

현재 자동 패커는 낮은 높이와 베드 접촉면을 우선했으며 overhang/support를 목적함수에 넣지 않았다.
따라서 `OneGrip_RightHand_Handle_plate_01_of_01.stl`을 그대로 출력하는 것은 권장하지 않는다.

## 5. 대안

### A. 권장 — 분리형 곡면 버튼 카세트

외부 그립 곡면은 유지하되 검지 4버튼의 포켓·retainer·스페이서를 쉘에 직접 통합하지 않는다.

- 쉘에는 곡면 창 1개와 M2 체결점 2개만 둔다.
- 4개 스위치를 한 개의 탈착식 곡면 카세트에 장착한다.
- 카세트 뒤쪽은 공용 나사식 커버로 막는다.
- 2.44mm 독립 스페이서 4개는 한 장의 TPU/silicone 압착 패드 또는 공용 backing plate로 통합한다.
- 배선은 PCB 없이 사전 납땜한 뒤 카세트 단위로 삽입한다.
- 권장 FDM 최소치: divider `≥1.2mm`, 구조벽 `≥1.8mm`, XY 여유 `0.25~0.30mm/side`.

장점은 쉘과 입력 모듈의 출력 방향을 따로 최적화할 수 있고, 실패 시 카세트만 다시 출력할 수 있다는 점이다.

### B. 빠른 연타용 — 2개 mouse switch + 2개 tactile 혼합

- 주 사격/조준 2개는 Omron D2F 계열 저압 lever 또는 mouse switch를 사용한다.
- 나머지 2개만 ITS-1105를 사용한다.
- mouse switch는 크지만 snap action과 긴 수명, 납땜 단자 선택지가 있어 PCB 없이 배선하기 쉽다.
- 4개 모두 mouse switch로 만들면 그립이 커지므로 혼합 구성이 적절하다.

### C. 엄지 단순화 — joystick + 4버튼 diamond

- HW-504 조이스틱은 유지한다.
- 8버튼 grid를 4개의 큰 버튼으로 줄여 joystick 아래에 diamond/arc로 배치한다.
- 나머지 기능은 검지·중지 버튼에 배분한다.
- 고정 파지 상태에서 모든 엄지 버튼이 joystick 중심 반경 약 25~30mm 안에 들어오도록 한다.

학습성, 촉각 구분, 출력 부품 수 면에서 가장 현실적이다.

### D. 엄지 다기능화 — 4방향+센터 푸시 모듈

Alps Alpine SKRH 같은 4방향+센터 푸시 부품은 약 `7.35×7.5×1.8mm`에 5입력을 제공한다.
공간 효율은 가장 좋지만 SMT라 소형 adapter PCB가 사실상 필요하므로 **PCB를 쓰지 않는 현재 방침에는 부적합**하다.

## 6. 권장 시제품 순서

1. 전체 30개를 출력하지 않는다.
2. 검지 I1-I2 두 버튼만 포함한 coupon을 `6.4 / 6.5 / 6.6 / 6.7mm` 포켓으로 출력한다.
3. 캡 clearance도 `0.20 / 0.25 / 0.30mm per side`로 비교한다.
4. 엄지는 조이스틱 장착부와 버튼 2개만 포함한 reach/fit coupon을 만든다.
5. 실제 스위치 lot 5개 이상을 캘리퍼로 측정한다.
6. 사용자가 10분 이상 파지한 상태에서 검지 4버튼 성공률과 엄지 8버튼 도달률을 측정한다.
7. 통과 후 큰 쉘은 눕힌 현재 방향 대신 약 `177mm` 높이의 세운 방향과 tree support를 먼저 슬라이스 비교한다.
8. 최종판은 쉘, backplate, 카세트/retainer, 캡·스페이서를 서로 다른 plate와 설정으로 출력한다.

## 7. 추천 최종 방향

현재 형상을 바로 양산형으로 보지 말고 **인체공학 외피 + 분리형 검지 카세트 + 단순화한 엄지 클러스터**로 한 번 더 정리하는 것이 좋다.

- 외피: 현재 유기적 grip 유지
- 검지: ITS-1105 4개를 공용 카세트로 통합
- 엄지: joystick + 4개 큰 버튼
- 주 연타 입력: 필요하면 Omron D2F-F 계열 1~2개만 혼합
- 출력: 큰 외피 FDM, 작은 정밀 카세트/캡은 0.2mm 노즐 FDM 또는 tough resin

## 8. 참고 자료

- OneGrip 내부 문서: `docs/01_original_button_mechanism_analysis.md`
- OneGrip 내부 문서: `docs/34_its1105_same_sku_8button_final.md`
- OneGrip 내부 문서: `docs/36_right_hand_handle_stl_print_plate.md`
- Omron D2F 공식 데이터시트: https://omronfs.omron.com/en_US/ecb/products/pdf/en-d2f.pdf
- Alps Alpine SKRH 공식 제품 페이지: https://tech.alpsalpine.com/e/products/category/muiti-control-devices/sub/02/series/skrh/
- Alps Alpine SKQJ 공식 카탈로그: https://tech.alpsalpine.com/assets/catalog/product-catalog-ta-all.en.pdf

## 9. 재현

로컬 감사 스크립트:

```powershell
& 'C:\Users\guest2\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' `
  'C:\Users\guest2\Documents\재활기기 공모전\analysis_onegrip_printability.py'
```

스크립트는 STL을 수정하지 않고 binary STL 형식, manifold edge, 퇴화 삼각형, 현재 출력 방향의 접촉면과
하향면을 읽기 전용으로 계산한다.
